Reverse an Array:

class Solution {
  public:
    void reverseArray(vector<int> &arr) {
        // code here
        vector<int>res;
        int n = arr.size();
        for(int i=n-1;i>=0;i--){
            res.push_back(arr[i]);
        }
        arr = res;
    }
};
